apiVersion: v1
kind: Service
metadata:
  name: {{ include "linker.fullname" . }}
  labels:
    {{- include "linker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "linker.selectorLabels" . | nindent 4 }}